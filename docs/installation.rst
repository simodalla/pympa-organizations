============
Installation
============

At the command line::

    $ easy_install pympa-organizations

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pympa-organizations
    $ pip install pympa-organizations
